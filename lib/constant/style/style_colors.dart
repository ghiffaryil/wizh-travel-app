import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.blue;
  static const secondary = Colors.deepOrange;
  static const alternative = Color.fromARGB(255, 34, 15, 1);
  static const danger = Colors.red;
}
